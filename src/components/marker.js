import {Marker, GoogleMap} from '@react-google-maps/api';
import React, {useState} from 'react'

function marker() {

const [marker, setMarker] = useState([])

const setMarkerIcon = (e)=>{

    setMarker((currentState)=> {[
        ...currentState,
        lat: 
        
        
        
        ]

    })

}

    return (
        <div>
            
        </div>
    )
}

export default marker
